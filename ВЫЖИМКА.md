cat > ВЫЖИМКА.md << 'EOF'
# 📋 ВЫЖИМКА ПРОЕКТА PL-1

**Дата обновления:** 19.09.2026

## 🎯 Проект
PL-1 — приложение для общения логистов с водителями в контексте доставки товаров.

**Стек:** FastAPI + SQLAlchemy (async) + PostgreSQL + Redis + Alembic + SQLAdmin + MAX Bot API + Docker

**Репозиторий:** github.com/Softinapro/PL-1.git (приватный)

## 📚 ТЕРМИНОЛОГИЯ
- **ДеньОтгрузки** — дата (обычно завтра). Контекст всей работы.
- **Trip** — задание водителю на ДеньОтгрузки (водитель + дата)
- **Route = Рейс** — маршрут (1 маршрут = 1 документ 1С «Рейс»)
- **Point** — торговая точка (магазин)
- **Логист** — работает ТОЛЬКО в 1С, в приложении НЕ создаётся
- **Водитель** — получает задание в MAX, подтверждает/отклоняет

## 🔄 БИЗНЕС-ЛОГИКА

### Основной поток
1. 1С создаёт заказы на доставку в торговые точки
2. Логист в 1С формирует маршруты (по географии + весу)
3. 1С отправляет в приложение через POST /import/trips (с X-API-Key)
4. Trip создаётся со статусом PENDING
5. Водитель получает уведомление в MAX
6. Водитель: Подтвердить всё / Отказаться от маршрута / точки / всего
7. 1С периодически запрашивает GET /export/trips?date=YYYY-MM-DD
8. 1С видит статусы (PENDING/CONFIRMED/REJECTED)
9. Логист в 1С пересобирает маршруты
10. 1С повторно импортирует через POST /import/trips → затирает старый Trip
11. Итерация до финала: все подтверждены

### Ключевые правила
- Дата отгрузки всегда уникальная (в системе активна одна дата)
- Точка в один день — в одном маршруте, в разные дни — в разных
- Вес приходит по каждой точке из 1С
- Новый импорт ЗАТИРАЕТ старый Trip (удаление + создание нового)
- **Отказы водителя НЕ хранятся отдельно** — только в статусах Point/Route/Trip
- **Дашборд логиста НЕ делаем** — 1С сама забирает данные через API
- **История отказов НЕ нужна** — всё в текущих статусах

## 📊 МОДЕЛИ БД

### User
- phone (String(20), unique, not null, index)
- full_name (String(100), not null)
- code (String(10), unique, nullable, index) — код из 1С (физлица)
- role (Enum: admin/logist/driver)
- is_active (Boolean, default True)
- is_archived (Boolean, default False)
- max_user_id (String(100), unique, nullable)

### DriverProfile
- user_id (FK users.id, unique, not null)
- max_user_id (String(100), unique, nullable)
- is_verified (Boolean)

### Trip (задание на ДеньОтгрузки)
- driver_id (FK users.id, not null, index)
- logist_id (FK users.id, nullable, index) — всегда None (логисты не создаются)
- logist_code (String(10), nullable, index) — код логиста из 1С
- date (Date, not null, index)
- status (Enum: PENDING/SENT/CONFIRMED/PARTIAL/REJECTED)
- sent_at (DateTime, nullable)
- info (String(300), nullable)

### Route (маршрут = рейс)
- trip_id (FK trips.id, not null, index)
- order_number (Integer, not null)
- name (String(255), nullable)
- status (Enum: PENDING/CONFIRMED/REJECTED)
- rejection_reason (Text, nullable)
- address_start (String(255), not null)
- address_end (String(255), not null)

### Point (торговая точка)
- route_id (FK routes.id, not null, index)
- code (String(10), not null, index) — код точки из 1С
- date (Date, not null, index) — дата отгрузки
- order_number (Integer, not null)
- address (String(255), not null)
- weight (Numeric(10, 3), nullable) — вес, кг
- status (Enum: PENDING/CONFIRMED/REJECTED)
- rejection_reason (Text, nullable)
- UniqueConstraint (code, date) — uq_point_code_date

### BaseModel
- id, created_at, updated_at

## 🔐 API-КЛЮЧ ДЛЯ 1С (IMPORT_API_KEY)

**Формат:** 64 hex-символа
**Хранение:** backend/.env (не коммитить)
**Заголовок:** X-API-Key
**Проверка:** verify_api_key в deps/auth.py
**Применяется:** для всех эндпоинтов 1С (import_drivers, import_trips, export_trips)

### Ротация
1. openssl rand -hex 32
2. Обновить в .env
3. docker compose restart backend
4. Обновить в 1С

## ✅ ЭНДПОИНТЫ

### Для 1С (X-API-Key)
- POST /import/drivers — импорт справочника водителей
- POST /import/trips — импорт рейсов (затирает старый Trip)
- GET /export/trips?date=YYYY-MM-DD — экспорт статусов на дату

### Для водителей (JWT)
- POST /auth/login — вход по телефону
- POST /auth/max/bind — привязка MAX (initData + phone)
- POST /webhook — вебхук MAX (кнопки)

### Служебные
- GET /ping — health check

## 📊 СТРУКТУРА ЭКСПОРТА (GET /export/trips)

Запрос:
GET /export/trips?date=2026-09-20
Header: X-API-Key: <ключ>

Ответ:
{
  "date": "2026-09-20",
  "trips": [
    {
      "trip_id": 5,
      "driver_code": "0001234567",
      "driver_name": "Водитель Сидоров",
      "driver_phone": "9001234567",
      "logist_code": "0008888888",
      "status": "pending",
      "info": "Обновлённый рейс",
      "routes": [
        {
          "route_id": 6,
          "name": "Новый маршрут",
          "order_number": 1,
          "status": "pending",
          "rejection_reason": null,
          "address_start": "Склад А",
          "address_end": "Склад Б",
          "points": [
            {
              "point_id": 9,
              "code": "T003",
              "order_number": 1,
              "address": "ул. Гагарина 10",
              "weight": "100.000",
              "status": "pending",
              "rejection_reason": null
            }
          ]
        }
      ]
    }
  ]
}

## ✅ ЧТО СДЕЛАНО

### Этап 1: Фундамент
- Docker + PostgreSQL + Redis + FastAPI
- Все модели (User, DriverProfile, Trip, Route, Point)
- SQLAdmin (админка, admin/admin)
- JWT (core/security.py)
- POST /auth/login
- POST /auth/max/bind
- POST /webhook

### Этап 2: Интеграция с 1С
- ✅ API-ключ для 1С (IMPORT_API_KEY)
- ✅ verify_api_key в deps/auth.py
- ✅ POST /import/drivers — импорт водителей
- ✅ POST /import/trips — импорт рейсов
- ✅ Затирание старого Trip
- ✅ Commit в цикле + rollback в except
- ✅ GET /export/trips — экспорт для 1С

### Этап 3: Интеграция с MAX (частично)
- Класс MaxBotAPI (send_message, send_notification)
- Вебхук MAX (приём сообщений и callback)
- Валидация initData
- URL MAX: platform-api2.max.ru

### Alembic миграции (все применены)
- 2189f832c645 — create all tables
- bfcb2b925624 — add max_user_id to user
- 2aa29bc05aba — add info/name/weight
- 5bebacc5ee17 — add code to user, make logist_id nullable
- 93ae0d59c7b6 — add logist_code to trip, code and date to point

### Протестировано ✅
- POST /import/drivers — создаёт/обновляет водителя
- POST /import/trips — создаёт Trip + Route + Point
- Затирание старого Trip работает
- Каскадное удаление Route/Point работает
- GET /export/trips — возвращает все данные со статусами

## ⏳ ПЛАН СЛЕДУЮЩЕЙ СЕССИИ

### Шаг 5: Уведомления в MAX (A.6)
- После commit в import_trips.py — отправить send_notification
- Одно уведомление на Trip
- Если нет max_user_id → записать в errors
- Метод send_notification уже есть в MaxBotAPI

### Шаг 6: Приём от водителя (кнопки в MAX)
- Кнопки «Подтвердить всё» / «Отказаться»
- Обновление статусов Point/Route/Trip
- Возможно: уведомление логисту (в 1С не нужно — 1С сама заберёт через /export)

### Шаг 7: Мини-приложение MAX
- Страница входа через initData
- Страница «Мои маршруты»
- Кнопки подтверждения/отказа
- JWT для API

### Шаг 8: SECRETS.md (отложено)
- Документация ротации ключа

## 📁 СТРУКТУРА (ключевое)

backend/
├── app/
│   ├── api/v1/
│   │   ├── deps/auth.py — get_current_user, require_role, verify_api_key
│   │   ├── endpoints/
│   │   │   ├── auth.py — login, max/bind
│   │   │   ├── import_drivers.py — POST /import/drivers
│   │   │   ├── import_trips.py — POST /import/trips
│   │   │   ├── export_trips.py — GET /export/trips
│   │   │   ├── webhook.py — POST /webhook
│   │   │   ├── driver.py
│   │   │   └── me.py
│   │   └── router.py
│   ├── core/
│   │   ├── security.py
│   │   └── integrations/
│   │       ├── max_bot.py (MaxBotAPI)
│   │       └── max_validator.py
│   ├── models/ (User, Trip, Route, Point, DriverProfile)
│   ├── utils/phone_utils.py (normalize_phone)
│   └── config.py
├── .env — реальный IMPORT_API_KEY
└── .env.example — заглушка

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Запуск
- docker compose restart backend
- docker compose exec backend alembic current
- docker compose exec backend alembic revision --autogenerate -m "..."
- docker compose exec backend alembic upgrade head

### Известные проблемы
1. WARN version obsolete — безвредно
2. black не установлен — ошибка post-write hook, не критично
3. echo=True в SQLAlchemy — много логов
4. Docker volume — иногда down && up -d

### URL MAX API
https://platform-api2.max.ru/bot

### Deep link
https://max.ru/{MAX_BOT_NAME}?startapp=trip_{trip_id}

### Формат телефонов
В БД: 9001234567 (10 цифр, без +7)
normalize_phone → 10 цифр, начинается с 9

### Формат кодов из 1С
Справочник Физические лица — String(10).
Используется для водителей И логистов.
nullable=True.

### Админка
- URL: http://localhost:8000/admin
- Логин: admin / admin (захардкожено)

## 📝 ФОРМАТ ОБУЧЕНИЯ
Пользователь в режиме обучения:
- Объяснять что и зачем перед кодом
- Идти пошагово
- Ждать подтверждения
- Не вываливать сразу много кода
- Пользователь сам вносит изменения

## 📌 ЧТО СКИНУТЬ В НОВОМ ЧАТЕ
1. backend/app/api/v1/endpoints/import_trips.py
2. backend/app/api/v1/endpoints/export_trips.py
3. backend/app/core/integrations/max_bot.py
4. backend/app/api/v1/endpoints/webhook.py

Продолжить с: Шаг 5 — уведомления в MAX после импорта.
EOF
echo "✅ ВЫЖИМКА.md перезаписана (актуальная версия от 19.09.2026)"